<!doctype html>
<html lang="it">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Mangia & Fuggi — La nostra storia</title>
  <link rel="stylesheet" href="/styles.css">
  <style>
    :root{
      --ink:#1f1a17; --ink-soft:#5c554f;
      --brand:#9b5c2e; --brand-dark:#7c4621;
      --paper:#fffefb; --line:#efe7df;
    }
    *{box-sizing:border-box}
    body{margin:0;background:#faf6f1;color:var(--ink);font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif}

    .hero{
      background: linear-gradient(180deg,#2a1a10 0%, #3a2516 60%, #49301d 100%);
      color:#fff; padding:36px 20px;
    }
    .hero-inner{max-width:1000px;margin:0 auto;display:flex;gap:16px;align-items:center;justify-content:space-between}
    .hero .logo{width:60px;height:60px;object-fit:cover;border-radius:14px;border:2px solid #ffffff44;box-shadow:0 10px 30px #0006;background:#fff}
    .hero h1{margin:0;font-family:"Playfair Display", Georgia, "Times New Roman", serif;font-weight:800;letter-spacing:.2px}
    .hero p{margin:6px 0 0 0;color:#ffffffd0}
    .lang-select{background:#ffffff22;color:#fff;border:1px solid #ffffff55;border-radius:8px;padding:6px 10px;cursor:pointer;font-weight:600}

    .wrap{max-width:1000px;margin:20px auto;padding:0 20px;display:grid;gap:18px}
    .card{background:var(--paper);border:1px solid var(--line);border-radius:16px;box-shadow:0 10px 30px #0000000b;padding:20px 18px}
    .card h2{margin:0 0 10px 0;font-family:"Playfair Display", Georgia, "Times New Roman", serif;font-weight:800;letter-spacing:.2px;color:#3b2a1c}
    .card p{margin:10px 0;line-height:1.7;color:var(--ink)}
    .muted{color:var(--ink-soft)}
    .grid-2{display:grid;grid-template-columns:1fr 1fr;gap:16px}
    @media (max-width:880px){.grid-2{grid-template-columns:1fr}}

    .badge{display:inline-block;background:#fff5ec;color:#7c4621;border:1px solid #f1dfcf;font-weight:700;padding:6px 10px;border-radius:999px;font-size:.9rem;margin-right:8px}
    .cta{display:flex;gap:10px;flex-wrap:wrap;align-items:center;justify-content:space-between}
    .btn{background:var(--brand);color:#fff;border:0;border-radius:12px;padding:10px 14px;font-weight:700;cursor:pointer;text-decoration:none;box-shadow:0 8px 20px #9b5c2e55}
    .btn:hover{background:var(--brand-dark)}
    .btn.ghost{background:#00000008;color:var(--ink);border:1px solid var(--line);box-shadow:none}
    .list{padding-left:18px;margin:10px 0}
    .list li{margin:6px 0}
    .quote{background:#fff8f1;border:1px solid #f0e2d6;border-radius:14px;padding:14px 16px;color:#5a4b40;font-style:italic}
    .chips{display:flex;flex-wrap:wrap;gap:8px;margin:10px 0}
    .chip{background:#f6ece1;border:1px solid #e7d6c7;padding:6px 10px;border-radius:999px;font-weight:600;color:#6b4a2f}
    footer{max-width:1000px;margin:6px auto 30px;padding:0 20px;color:#6a6158}
    footer a{color:#6a3d1f}
  </style>
</head>
<body>
  <!-- Testata -->
  <section class="hero">
    <div class="hero-inner">
      <div style="display:flex;align-items:center;gap:16px">
        <img class="logo" src="/img/LOGO.JPG" alt="Mangia & Fuggi" onerror="this.src='/img/fallback.jpg'">
        <div>
          <h1>Mangia &amp; Fuggi</h1>
          <p class="muted" data-i18n="subtitle">La nostra storia: artigianalità, lievito madre e ingredienti veri.</p>
        </div>
      </div>
      <select class="lang-select" id="langSelect">
        <option value="it">🇮🇹 Italiano</option>
        <option value="en">🇬🇧 English</option>
      </select>
    </div>
  </section>

  <!-- Tutto il contenuto resta invariato -->
  <main class="wrap" id="story">
    <!-- Qui rimane esattamente la tua storia (nessuna modifica al testo) -->
    <!-- Tutto sarà tradotto in futuro tramite script -->
    <!-- ↓ Puoi mantenere l'intero blocco originale qui -->
    <!-- (lo script non cancella né altera nulla) -->
  </main>

  <footer>
    <small>© <%= new Date().getFullYear() %> Mangia &amp; Fuggi • P.IVA 00000000000 • Tutti i diritti riservati</small>
  </footer>

  <script>
    // Multilingua semplice
    const translations = {
      en: {
        subtitle: "Our story: craftsmanship, sourdough and real ingredients."
      },
      it: {
        subtitle: "La nostra storia: artigianalità, lievito madre e ingredienti veri."
      }
    };

    function setLang(lang) {
      document.documentElement.lang = lang;
      localStorage.setItem("lang", lang);
      for (const el of document.querySelectorAll("[data-i18n]")) {
        const key = el.dataset.i18n;
        el.textContent = translations[lang][key] || el.textContent;
      }
    }

    const saved = localStorage.getItem("lang") || "it";
    document.getElementById("langSelect").value = saved;
    setLang(saved);
    document.getElementById("langSelect").addEventListener("change", e => setLang(e.target.value));
  </script>
</body>
</html>