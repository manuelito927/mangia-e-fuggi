<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>Mangia & Fuggi — Login Cameriere</title>

  <link rel="stylesheet" href="/styles.css">
  <link rel="stylesheet" href="/pos.css">

  <style>
    body{margin:0;min-height:100vh;display:flex;align-items:center;justify-content:center;background:#f6efe7}
    .card{width:min(420px,92vw);background:#fff;border-radius:16px;padding:20px;box-shadow:0 10px 30px rgba(0,0,0,.10)}
    .title{font-weight:800;font-size:20px;margin:0 0 8px;color:#2c2623}
    .sub{color:#7b6d66;margin:0 0 16px;font-size:14px}
    .row{display:flex;gap:10px}
    input{flex:1;padding:12px 14px;border-radius:12px;border:1px solid #ddd;font-size:16px}
    button{padding:12px 14px;border-radius:12px;border:0;cursor:pointer;font-weight:800;background:#7a4a3b;color:#fff}
    .err{margin-top:12px;color:#b42318;font-size:14px}
  </style>
</head>
<body>
  <div class="card">
    <h1 class="title">Accesso Cameriere</h1>
    <p class="sub">Inserisci il PIN per aprire la schermata cameriere.</p>

    <form method="POST" action="/app">
      <div class="row">
        <input
          name="pin"
          type="password"
          inputmode="numeric"
          autocomplete="one-time-code"
          placeholder="PIN cameriere"
          required
        />
        <button type="submit">Entra</button>
      </div>

      <% if (error) { %>
        <div class="err"><%= error %></div>
      <% } %>
    </form>
  </div>
</body>
</html>